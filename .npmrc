auto-install-peers=true
strict-peer-dependencies=false
# cPanel deploy account has NPROC capped at 100 (keep well under 80) — cap
# concurrent install/postinstall child processes so `pnpm install` never
# spikes past that alongside Apache/Passenger/cron on the shared account.
child-concurrency=4
